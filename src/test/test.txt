
function main() = {
	
	number numero_de_prueba
	numero_de_prueba = 2

	character caracter_de_prueba = 'c'
	
	boolean booleano_de_prueba
	booleano_de_prueba = true
	
	array[character] arreglo_de_prueba
	
	array[array[number]] matriz_de_prueba

	if (id < numero) then {
		if (id == numero) then {
		
		}else if (id <> numero) then {
		
		}else{
		
		}
	}else{
		while (id >= numero){
			switch (id) {
				case (1):{
				}
				default:{
				}
			}
		}
	}
	for (id2 = id to 10 | 1 ){
	
	}
	
	id = input()
	output(id)
    call llamada_funcion()
    call llamada_funcion(variable1)
    call llamada_funcion(variable1,variable2)
}

function funcion_sin_parametros()={
}

function funcion_con_parametros(numero1:number, numero2:number, numero3:number) = {

}

function funcion_con_retorno() = number{
	comeBack empty
}

function funcion_con_parametros_retorno(numero1:number, numero2:number, numero3:number) = number {	
	comeBack empty
}


